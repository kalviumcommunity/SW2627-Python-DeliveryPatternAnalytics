services:
  - type: web
    name: delivery-pattern-analytics
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run scripts/app.py --server.port $PORT --server.address 0.0.0.0